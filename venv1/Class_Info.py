# The first student represents the file saved in the memory while the second student represents
# the class within the file.
from Student import Student
student1 = Student("Jim", "Computer Science", 3.3, False)

print(student1.name)
print(student1.on_honor_roll())